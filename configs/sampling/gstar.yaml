defaults:
  - starshape  # Inherit from StarShape config

# Override sampler target to use GStarSampler
sampler:
  _target_: discrete_diffusion.sampling.gstar.GStarSampler
  # Temperature for remasker-guided sampling (0 = deterministic top-k, higher = more stochastic)
  remasker_temperature: 1
