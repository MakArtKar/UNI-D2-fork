# @package _global_

defaults:
  - starshape
  - override /sampling: gstar

checkpoint_path: outputs/owt/gstar_len${max_length}/dummy_checkpoints/checkpoints/best.ckpt
samples_path: ${base_samples_path}/gstar_t_on_${t_on}_remasker_schedule_${remasker_schedule}_len_${max_length}.pt
