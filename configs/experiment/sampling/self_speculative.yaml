# @package _global_

defaults:
  - /experiment/sampling/mdlm@_global_
  - override /sampling: self_speculative

checkpoint_path: outputs/owt/self_speculative_len${max_length}/dummy_checkpoints/checkpoints/best.ckpt
samples_path: ${base_samples_path}/self_speculative_${sampling.mode}_${sampling.window_schedule}_dt${sampling.cosine_delta_t}_len_${max_length}_steps_${num_steps}.pt
