# @package _global_

defaults:
  - mdlm
  - override /algo: gstar

trainer:
  max_steps: 50_000

training_finetune_path: outputs/owt/mdlm_finetune_len${model.length}/dummy_checkpoints/checkpoints/last.ckpt

hydra:
  run:
    dir: ./outputs/${data.train}/gstar

wandb:
  name: gstar_${data.train}
